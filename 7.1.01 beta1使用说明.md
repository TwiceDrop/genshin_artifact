# 莫娜占卜铺 7.1.01 beta1

双击本目录的 **启动7.1.01 beta1.bat**，打开 `http://127.0.0.1:4183/#/calculate`。此版本在本地 beta3 的基础上更名发布，WASM 与生成元数据随源码提供。首次从 GitHub 下载后，使用 Node.js 22 LTS 运行 `npm ci` 和 `npm run build:local`，然后双击启动。

旧版本保留。此版本使用4183端口，账号数据保存在当前目录的 `.local-data`；如需原有数据，在旧版的「UID 数据」导出，再到此版本导入。旧预设的蝶变开关会按关闭=0%、开启=100%迁移。

已取消沃雅妮莎的大幅测试占位背景；角色背景图限制在左侧角色栏内，避免遮挡配置和圣遗物区域。

## 新专武设置

选择沃雅妮莎或薇斯纳时默认选中各自专武。两把武器都支持精炼1～5、1～90级与20/40/50/60/70/80级突破前后。基础攻击沿用工程的游戏显示整数口径，副词条保留四位小数；新专武改用逐级数据，避免原内核的区间直线插值误差。

### 漩流颂歌

- 「特效来源最终生命值」填0：随装备、等级、武器生命加成、角色命座与BUFF自动计算。
- 填写正数：以此数值计算专武的生命转攻击。输入已经包含生效中的武器、命座等生命加成，不再重复添加；不会改写装备者自身生命、治疗量或角色天赋的生命来源。
- 可选0～3层蜜酿、冻结/星扩散强化状态、装备者是否在场。后台仍可获得武器生命和治疗加成，但自身不享受仅提供给前台的攻击加成。
- 为其他角色计算沃雅妮莎的支援，添加她分组里的「漩流颂歌」BUFF，输入来源最终生命、精炼、层数、强化状态。不要与同一装备者已经启用的专武自身加攻重复添加。

精炼1～5的治疗与每层生命加成为4%/5%/6%/7%/8%。每层加攻按最终生命超过40000的部分计算，每1000生命提供0.4%/0.5%/0.6%/0.7%/0.8%，每层上限8%/10%/12%/14%/16%；达到60000生命即到上限。强化将每层生命和攻击效果乘1.75。

### 蝶变

三项覆盖率均可独立填写0～100%，无需相加为100%；前两种持续效果可以共存。

| 效果 | 精炼1 / 2 / 3 / 4 / 5 | 覆盖率计算 |
|---|---|---|
| 忠忱之风 | 暴伤56% / 72% / 88% / 104% / 120% | 乘暴伤覆盖率，进入面板与伤害 |
| 叛弃之风 | 星扩散增伤36% / 45% / 54% / 63% / 72% | 乘星伤覆盖率，仅进入星扩散乘区 |
| 丰获之风 | 回复5 / 5.5 / 6 / 6.5 / 7能量 | 乘回能覆盖率，显示等效每4秒回能 |

前两项采用平均属性模型，单人配装和词条收益共用此设置。它不模拟BUFF与各次命中的时间分布，也不是整轮DPS。回能覆盖率定义为每4秒触发机会的利用率，实际仍需按E/Q顺序触发；直接回能不受充能效率放大，不额外提高伤害或充能面板。退场时三项效果均清除，武器基础攻击与副词条保留。

## 保留的 beta2 范围

本次专武装备计算对应沃雅妮莎、薇斯纳。两把专武装备到其他角色的完整计算仍未接入；沃雅妮莎专武的队友加攻BUFF可用于已有角色。没有把旧角色强制切到较早的扩展角色实现，以免丢失旧角色的命座和技能差异。

角色数据仍沿用 beta2 已核对的7.0.54 / D48100502；本次9月19日重新读取的武器数据页也标记此版本。星扩散与整肃乘区、角色完整时间轴、多人联合配装及未支持套装仍保留 beta2 的限制，不将测试服数据标成正式服实测结论。

## 数据与复跑

- [蝶变数据](https://gi.gachabase.net/weapons/11522/beyond-the-chrysalis/beta?lang=chs)
- [漩流颂歌数据](https://gi.gachabase.net/weapons/14524/hymn-of-the-maelstrom/beta?lang=chs)
- 页面快照：`beta-data/sources-beta3/`；精炼、192组等级/突破数据及来源SHA256：`beta-data/weapons-beta3.json`。
- `npm run test:local`：69项本地回归。
- `npm run test:beta`、`npm run test:beta2`：原14项、15项内核检查。
- `npm run test:beta3`：20项专项，包含192组等级/突破及200组精炼/生命/层数/强化组合。
- `npm run build:beta-kernel`：重新编译扩展并运行三组内核检查；需要说明中原有Rust、MinGW、wasm-bindgen工具链。
- `npm run build:local`：更新电脑网页。

结果见 `beta-data/beta3-tests.json` 与 `beta-data/release-validation.json`。构建和服务日志只保留在本地，不上传。本次不生成安卓APK。

发布整理时沿用仓库 V7.0.11 已有的喵喵评分实现、扫码模块许可与 MONA_DATA_DIR 支持。

