# 莫娜占卜铺 7.1.06 使用说明

Windows 安装包自带 Node.js。网页包解压后，安装 Node.js 22 LTS 或更新版本，双击 **启动7.1.06.bat**，再打开 `http://127.0.0.1:4183/#/calculate`。从源码运行时，先执行 `npm ci`、`npm run build:local`，然后执行 `node script/start-local.mjs`。安卓本地版的安装及 UID 数据互传见[安卓使用说明](docs/android-offline.md)。

## 更新检查

网页版和 Android App 启动时会联网检查本项目最新的 GitHub Release。发现更高版本后，弹窗显示版本更新日志；选择「否」后，下次启动仍会提示。勾选「不再自动提示更新」可关闭启动检查，「关于」页面的「检查更新」按钮仍可随时手动查询。无法联网时，已有本地计算功能仍可使用。

网页端点击「前往更新」会打开 GitHub Release 页面；Android 端点击后会通过外部浏览器打开 APK 下载地址，下载完成后需按系统提示安装。应用不会静默下载或安装。升级前建议从「UID 数据」导出备份。

## 装备与计算范围

角色、武器、圣遗物、伤害目标及配装入口沿用 7.1.04 的配置方式。本版扩充薇斯纳的七把单手剑与沃雅妮莎的八把法器的数据及部分计算路径；装备仍须符合角色的武器类型。四套新增圣遗物中有三套尚未完整接入，不应将目录中可选择视为效果均可计算。具体装备、效果与计算边界见[更新说明](7.1.06更新说明.md)。

效果覆盖率按平均属性折算，不模拟逐次命中和持续时间。直接回能显示触发数值，不自动改动充能效率或队伍轮转。单次伤害也不代表完整队伍轮转 DPS。需要触发条件的效果应按实际队伍与技能状态设置；界面标记「公式待校准」的结果不能当成零伤害或用于配装结论。旧角色使用新武器的 1～89 级白值路径仍被阻断，不能按 90 级结果外推。沃雅妮莎的月反应与部分星扩散入口仍待校准；歌声 Q 的 86.4% 暂按增伤区试算，必须手动确认状态，尚缺控制变量验证，不会自动开启。

直接星扩散已调整额外提升顺序，但队伍合成仍需提供实际参与者；缺少这些输入时不能把单人结果作为队伍总伤。相关数值快照更新与独立样例验收仍在进行。

旧版说明与既有数据迁移规则仍可查阅[7.1.04 使用说明](7.1.04使用说明.md)。本次发布提供源码、Windows 安装包、网页包与[安卓 APK](https://github.com/TwiceDrop/genshin_artifact/releases/tag/v7.1.06)。原项目许可及第三方声明见 `LICENSE`、`THIRD_PARTY_NOTICES.md`。


## 7.1.06 队友与 BUFF 修复

自动来源新增奥黛塔、七七和桑多涅。奥黛塔分别配置辉映、受益角色实际华彩层数、独舞倒影和雪鹄之梦状态；按来源命座和游戏显示爆发等级解锁。华彩转移仍由用户填写当前状态，不模拟时间轴。辅助之间不能占用同一件圣遗物。

旧角色可以使用90级新增目录武器，沿用原发布角色内核；1～89级仍明确阻断。部分新武器的旧角色路径只支持完整覆盖率，纺夜天镜同时触发两个队友反应状态仍阻断。

这次补齐的是 extension-buffs.mjs 列出的效果，不能将本版理解为全部256项 BUFF 或全部星/月反应均已适配。
