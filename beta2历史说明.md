# 莫娜占卜铺 beta2

双击本目录的 **启动beta2.bat**。地址是 `http://127.0.0.1:4182/#/calculate`。

beta2 位于 `D:\Documents\ChatGPT\beta2`。原程序（4174）和 beta1（4178）均保留，使用不同的端口、浏览器数据库和本地数据目录。要继续使用原有角色及装备，在原版或 beta1 导出对应 UID 数据包，再到 beta2 的「UID 数据」页面导入。不会修改或搬走原版的装备。

## 薇斯纳

在角色选择器的「风」分类选择「薇斯纳」。武器列表加入「蝶变」；其暴伤和星伤开关表示当前命中时仍然生效的状态，不会假设第一次 E 就获得两种效果。薇斯纳自身被动、命座效果由角色配置直接控制，避免再次添加同一自BUFF造成重复加成。

采用已确认的测试服 **7.0.54 / D48100502**。二命满层攻击加成为40%；蝶变精1为56%暴伤、36%星扩散增伤。

- 普通技能、Q灵剑、六命条目与专武已注册到原生 Rust/WASM 扩展。
- 灵剑按「辉映·星扩散」切换普通风伤和直接星扩散，初始E、本体剑击和风翎不转换。三阶灵剑第一项为单次伤害，总共四次，最终段单列。
- 二命且处于列装时自动使用六层整肃；其他情况下使用填写的命中时层数。辉映仍需先触发冰扩散，薇斯纳和沃雅妮莎两人无法自行提供冰附着。
- 目标「薇斯纳-灵剑·爆发」优化单次 Q 期望伤害，随辉映切换；单人配装、十词条收益和0～20条最优收益曲线共用原生计算。它不是整轮或整队 DPS。
- 天赋填游戏显示等级（含命座）。米游社导入按角色/技能名称匹配，C3的E13读取为内核索引12，不再加三。
- 米游社尚无账号内新角色数据时，可以手动选择角色。不会伪造账号拥有的角色。
- 评分保留喵喵原有权重；薇斯纳没有对应权重时显示待适配，不使用编造的毕业分数。

## 沃雅妮莎支援

BUFF 选择窗口的沃雅妮莎分组新增「最后的塑诗者」，只在流荡风旋创建/引爆后的减抗有效时间内添加。选择「十二弦的泪歌」或二命暴伤时，关闭「普通水/冰模式」才能启用星扩散对应增益。基础生命输入来源角色的最终生命，不重复添加她的装备或命座生命。

辅助增益按单次有效命中处理；用完领唱/重唱或超出持续时间后应关闭对应 BUFF。没有自动模拟时间轴、群怪扣层和风旋生成次数。

## 测试服边界

「定额加值也乘整肃」是两种尚待实测模型的选择。默认先乘整肃再加队友定额值；打开则定额值也乘整肃。两种模型均与上一轮独立实验一致，但这不代表游戏结算顺序已经获得实测确认。基础星伤与辅助定额值、多个擢升来源、二次属性转换也仍需正式服校准。

血红之证与炉火融炼之心已加入扩展的原生套装计数；已有公开 Rust 支持的套装继续可用。**薇斯纳配装候选暂时排除扩展尚未支持的套装**，界面有提示；手动装备含未支持的两件/四件效果时会明确报错，不以错误被动算出结果。旧角色仍用原发布内核，不受此候选限制。扩展支持列表见 `beta-data/extension-support.json`。

目前蝶变仅随薇斯纳接入；为其他角色选择蝶变会明确提示待适配。薇斯纳的 MONA-DSL、多人联合配装、自然触发星扩散/风旋的整轮结算仍未接入；原角色对应功能保留。此次没有构建手机 APK。

## 验证和构建

- 60项本地回归检查通过。
- 14项沃雅妮莎原生检查通过。
- 15项 beta2 检查通过，包含真实瑞希装备复算、两种星伤模型、四件套计数、配装目标一致性和实际0～20条收益曲线。

结果分别位于 `beta-data/regression-beta2.log`、`vody-beta2.log`、`beta2-tests.json`；曲线样例在 `vesna-curve.json`。程序没有自动向账户写入测试角色或装备。

`npm run build:local` 更新电脑网页；`npm run test:beta2` 复跑 beta2 检查。Rust 编译入口为 `beta-tools/build-extension.ps1`，需要现有 nightly-2024-10-10、wasm32目标、MinGW与wasm-bindgen工具链。已随程序提供编译好的WASM，正常使用不需要重新编译。

角色及武器图片来自 Gachabase 对应测试服条目，已放入本地 `public/beta`。数据快照在 `beta-data/vesna.json`；原发布WASM保留，新的扩展WASM只用于已接入的新角色。

## UID 导出、导入撤销与伤害对比更新

伤害表的期望、暴击、非暴击伤害共用圣遗物对比基准（游戏内穿戴／此次计算前／对比历史），显示「之前 → 当前（差值，变化百分比）」，红增绿减。百分比按未取整的原始伤害计算，仅用于伤害表；基准为0且当前不为0时显示百分比不适用。角色、武器、BUFF、技能与敌人均保持当前条件，仅替换基准圣遗物重算。关闭对比或无基准时只显示当前值。

「导出此 UID」包含该 UID 的已穿戴及闲置圣遗物。UID 数据包导入和米游社同步会保留装备归属，避免混入其他 UID；旧 YAS／OCR 数据没有 UID 标记时，通过默认勾选的「附带未标记 UID 的仓库圣遗物」一起导出。导出按钮下方显示件数。

文件导入与米游社同步会生成本机导入记录，刷新或重启后仍可撤销。同一 UID 从最新记录向前撤销。撤销仅删除本次实际新增的非重复圣遗物，去重复用的原有装备不会删除；后来修改、被预设／套装／其他记录引用的新增装备也保留。角色和预设仅在仍与导入时一致时恢复，后续修改会保留并提示。更新前没有记录的导入不能追溯撤销。

本次 69 项本地检查通过；浏览器实测首次导入20件、重复导入复用20件，撤销重复导入移除0件；刷新后撤销首次导入正确移除20件。测试使用独立端口和合成数据，未操作实际 UID 的仓库。
